# KDL Cyclone 0.19.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer un correctif 0.19.1 où les sargasses et le sable sont immédiatement compréhensibles, Météo-France est visuellement prioritaire et l'accueil ne présente plus aucun chevauchement.

**Architecture:** Conserver le collecteur et le canvas existants. Corriger les états de calques dans `app.js`/`carte.js`, appliquer une palette non interpolée dans `satellite.js`, puis rendre la hiérarchie des sources explicite dans la page territoire. Étendre la QA Chrome/CDP existante pour contrôler les interactions et les géométries réelles.

**Tech Stack:** Node.js ESM, JavaScript navigateur sans framework, Canvas 2D, CSS responsive, tests `node:test`, Chrome DevTools Protocol.

## Global Constraints

- Conserver le visuel 0.19.0 validé et la branche `refonte/cyclone`.
- Ne jamais lire, copier ni afficher `METEOFRANCE_API_TOKEN`.
- Météo-France est prioritaire ; Open-Meteo n'apparaît qu'en secours explicite.
- Sable : jaune/ocre translucide. Sargasses : bandes vert foncé.
- Aucune donnée absente ne devient zéro et aucune interpolation ne crée de fausse précision.
- Aucun chevauchement ni débordement à 360, 390, 768, 1280 et 1440 px, y compris avec zoom texte 200 %.
- Aucun déploiement avant sauvegarde vérifiée et feu vert explicite de Karim.

---

### Task 1: Activation fiable des sargasses

**Files:**
- Modify: `public/js/app.js:2502-2518,3619-3655`
- Modify: `public/js/carte.js:68-80,895-932`
- Create: `test/sargasses-client.test.js`

**Interfaces:**
- Consumes: `Carte.definirSargasses({date, points})` et `Carte.definirCalque(nom, actif)`.
- Produces: premier chargement actif, état cohérent après erreur, sargasses chargées automatiquement à l'ouverture de la carte.

- [x] **Step 1: Écrire le test rouge du premier chargement**

```js
assert.deepEqual(appels, ['donnees', 'calque:sargasses:true']);
```

- [x] **Step 2: Vérifier l'échec attendu**

Run: `node --test test/sargasses-client.test.js`
Expected: FAIL, seul l'appel `donnees` est observé.

- [x] **Step 3: Activer la couche après réception**

```js
carte.definirSargasses(donnees);
carte.definirCalque('sargasses', true);
```

- [x] **Step 4: Vérifier le test ciblé**

Run: `node --test test/sargasses-client.test.js`
Expected: PASS 1/1.

- [ ] **Step 5: Tester puis rendre l'état initial et l'échec cohérents**

Ajouter aux tests de source les attentes suivantes avant le code :

```js
assert.match(carteSource, /sargasses:\s*true/);
assert.match(appSource, /\['sargasses', 'Sargasses', true\]/);
assert.match(blocErreur, /definirCalque\('sargasses', false\)/);
```

Puis définir `sargasses: true` dans `Carte`, cocher l'entrée initiale et appeler
`chargerSargasses()` depuis `initialiserCarte()`. Dans le `catch`, décocher la
case et appeler `carte.definirCalque('sargasses', false)`.

- [ ] **Step 6: Vérifier et committer**

Run: `node --test test/sargasses-client.test.js test/couches-environnement.test.js`
Expected: PASS.

```bash
git add public/js/app.js public/js/carte.js test/sargasses-client.test.js
git commit -m "fix(carte): afficher les sargasses au premier chargement"
```

---

### Task 2: Palettes visuelles sable et sargasses

**Files:**
- Modify: `public/js/satellite.js:20-45,270-295`
- Modify: `public/js/app.js:2478-2500,3640-3655`
- Modify: `public/js/carte.js:901-929`
- Modify: `public/css/app.css:2023-2056`
- Modify: `test/couches-environnement.test.js`

**Interfaces:**
- Produces: `KdlSatellite.filtrePalette('sable') -> string`, option `palette: 'sable'`, capsules NOAA vert foncé dont largeur/opacité portent le risque.

- [ ] **Step 1: Écrire les tests rouges de palette**

```js
vm.runInNewContext(satelliteSource, contexte);
assert.equal(contexte.window.KdlSatellite.filtrePalette('sable'),
  'sepia(1) saturate(1.55) hue-rotate(350deg) brightness(.96) contrast(1.08)');
assert.match(carteSource, /#123f2b/);
assert.doesNotMatch(blocSargasses, /#d2a13a|#e56f2f/);
```

Run: `node --test test/couches-environnement.test.js`
Expected: FAIL sur les nouvelles couleurs/fonction.

- [ ] **Step 2: Ajouter la palette sable sans modifier les données**

```js
function filtrePalette(nom) {
  return nom === 'sable'
    ? 'sepia(1) saturate(1.55) hue-rotate(350deg) brightness(.96) contrast(1.08)'
    : 'none';
}
```

Le constructeur accepte `options.palette` et `options.opacite`. `dessiner()`
pose `ctx.filter = filtrePalette(this.palette)` avant `drawImage`. Le chargement
de sable passe `{ palette: 'sable', opacite: 0.72, masque: false }`. Exposer
`filtrePalette` dans `window.KdlSatellite` afin que le comportement reste testé
sans démarrer un navigateur.

- [ ] **Step 3: Remplacer la palette sargasses**

```js
var verts = ['#2f6b3c', '#1f5935', '#123f2b'];
ctx.fillStyle = verts[risque - 1];
ctx.globalAlpha = 0.58 + risque * 0.1;
ctx.ellipse(0, 0, rayon * (2.1 + risque * .25), rayon * .62, 0, 0, Math.PI * 2);
```

Ajouter un contour fin et `risque` traits/anneaux discrets afin que l'intensité
reste compréhensible sans dépendre uniquement de la couleur.

- [ ] **Step 4: Vérifier et committer**

Run: `node --test test/couches-environnement.test.js test/sargasses-client.test.js`
Expected: PASS.

```bash
git add public/js/satellite.js public/js/app.js public/js/carte.js public/css/app.css test/couches-environnement.test.js
git commit -m "feat(carte): distinguer sable et sargasses"
```

---

### Task 3: Priorité visible de Météo-France

**Files:**
- Modify: `public/js/app.js:1205-1415`
- Modify: `public/css/app.css:1360-1390`
- Create: `test/meteo-priorite-client.test.js`

**Interfaces:**
- Produces: `strategieSourcesMeteo(territoire) -> {observationsMf, secoursConditions}`.
- Consumes: `territoire.observations.disponible`, `.perime`, `territoire.conditions`, `territoire.mer`.

- [ ] **Step 1: Écrire les tests rouges de sélection**

```js
assert.deepEqual(strategieSourcesMeteo({ observations: { disponible: true, perime: false } }), {
  observationsMf: true, secoursConditions: false,
});
assert.equal(strategieSourcesMeteo({ observations: null }).secoursConditions, true);
assert.equal(strategieSourcesMeteo({ observations: { disponible: true, perime: true } }).secoursConditions, true);
```

Run: `node --test test/meteo-priorite-client.test.js`
Expected: FAIL car la fonction n'existe pas.

- [ ] **Step 2: Implémenter la stratégie pure**

```js
function strategieSourcesMeteo(terr) {
  var obs = terr && terr.observations;
  var observationsMf = Boolean(obs && obs.disponible);
  return { observationsMf: observationsMf,
    secoursConditions: !(observationsMf && !obs.perime) };
}
```

- [ ] **Step 3: Appliquer la hiérarchie dans la page territoire**

Conserver les relevés Météo-France en premier. N'afficher le bloc de conditions
Open-Meteo que si `secoursConditions` vaut `true`, avec le badge et le texte
exact `Secours Open-Meteo`. Séparer la mer et les cinq jours dans des blocs
également marqués `Secours Open-Meteo Marine` et `Secours Open-Meteo`, après le
lien officiel Météo-France/ARPEGE. Ne fusionner aucun champ entre ces blocs.

```js
var strategieMeteo = strategieSourcesMeteo(terr);
if (strategieMeteo.secoursConditions && c) {
  html += '<div class="carte-bloc"><span class="etiquette etiquette--secours">Secours Open-Meteo</span>';
}
if (mer) {
  html += '<div class="carte-bloc"><span class="etiquette etiquette--secours">Secours Open-Meteo Marine</span>';
}
```

- [ ] **Step 4: Vérifier et committer**

Run: `node --test test/meteo-priorite-client.test.js test/vigilance-client.test.js`
Expected: PASS.

```bash
git add public/js/app.js public/css/app.css test/meteo-priorite-client.test.js
git commit -m "fix(meteo): rendre le secours Open-Meteo explicite"
```

---

### Task 4: Chevauchements et passe UI/UX finale

**Files:**
- Modify: `scripts/qa-navigateur.mjs:130-230`
- Modify: `public/css/app.css:2938-3115`
- Modify: `public/js/app.js:817-840,3619-3655`

**Interfaces:**
- Produces: QA géométrique sur `.situation__eyebrow`, `.situation__etat`, `.situation__titre`, `.situation__detail`, `.cartouche`, `.situation__pied` et commandes de calques enrichies.

- [ ] **Step 1: Ajouter le contrôle rouge des intersections**

Dans la QA CDP, mesurer les rectangles visibles et remonter toute intersection :

```js
const chevauchements = await cdp.evaluer(`(() => {
  const els = [...document.querySelectorAll('.situation__eyebrow,.situation__etat,.situation__titre,.situation__detail,.cartouche,.situation__pied')]
    .filter((e) => e.offsetParent !== null).map((e) => ({ s: e.className, r: e.getBoundingClientRect().toJSON() }));
  return els.flatMap((a, i) => els.slice(i + 1).filter((b) =>
    a.r.left < b.r.right && a.r.right > b.r.left && a.r.top < b.r.bottom && a.r.bottom > b.r.top
  ).map((b) => a.s + ' / ' + b.s));
})()`);
```

Exécuter aux largeurs 360, 390, 768, 1280 et 1440, puis répéter à 390 après
`Emulation.setPageScaleFactor` avec `pageScaleFactor: 2`. Expected avant correction : au
moins une intersection reproduit le retour de Karim.

- [ ] **Step 2: Corriger le flux responsive**

Remplacer la marge négative desktop par un espacement positif :

```css
@media (min-width: 620px) {
  .situation__eyebrow { grid-column: 1 / -1; margin-bottom: var(--e3); }
}
.situation__titre, .situation__detail, .calques label {
  min-width: 0;
  overflow-wrap: anywhere;
}
```

Conserver tout le texte visible : aucune règle `display:none`, hauteur fixe ou
troncature n'est ajoutée aux six blocs contrôlés.

- [ ] **Step 3: Réaliser la finition visuelle**

Ajouter aux entrées Sable/Sargasses une pastille jaune-ocre ou vert foncé, un
état actif net, la source NOAA et la date dans le message d'état. Garder des
animations sous 200 ms et les désactiver avec `prefers-reduced-motion`.

- [ ] **Step 4: Vérifier les vues et committer**

Run: `KDL_QA_BASE=http://127.0.0.1:4240 npm run qa`
Expected: aucune intersection à 360/390/768/1280/1440 et zoom 200 %, aucun
débordement, aucune exception console ; captures mobile et desktop créées.

```bash
git add scripts/qa-navigateur.mjs public/css/app.css public/js/app.js
git commit -m "fix(ui): supprimer les chevauchements et clarifier les calques"
```

---

### Task 5: Validation et préparation 0.19.1

**Files:**
- Modify: `package.json`
- Modify: `public/version.json`
- Modify: `PROJECT_STATE.md`

- [ ] **Step 1: Lancer les validations complètes**

Run: `npm test`
Expected: tous les tests réussissent.

Run: `node --check public/js/app.js && node --check public/js/carte.js && node --check public/js/satellite.js && git diff --check`
Expected: sorties vides et codes 0.

- [ ] **Step 2: Contrôler visuellement les captures**

Vérifier les captures à 390 et 1440 px : sable jaune/ocre, sargasses vert foncé,
source/date visibles, titre d'accueil sans superposition, contrastes lisibles.

- [ ] **Step 3: Passer la version à 0.19.1 et consigner le checkpoint**

Mettre `package.json` et `public/version.json` à `0.19.1`, puis documenter les
tests, les captures, le commit et le plan de retour arrière dans `PROJECT_STATE.md`.

- [ ] **Step 4: Commit de release locale**

```bash
git add package.json public/version.json PROJECT_STATE.md
git commit -m "chore(release): preparer KDL Cyclone 0.19.1"
```

- [ ] **Step 5: Arrêt avant production**

Présenter à Karim les résultats, la cible VPS vérifiée, la commande de sauvegarde
horodatée avec SHA-256, les fichiers à transférer et la commande de rollback.
Attendre son feu vert explicite avant sauvegarde/déploiement/restart PM2.
